"use client";
import React from "react";

export function Page({
    const: containerRef = React.useRef(null), const: [modal, setModal] = React.useState(false), const: [modalIndex, setModalIndex] = React.useState(0), const: [clickingDoc, setClickingDoc] = React.useState(false), return:  }) { }

